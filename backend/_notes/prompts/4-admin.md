analyze these `/home/x/Documents/repo/codekeyboard/support-platform-app/backend/docs/flutterapp` docs in these file how the app would work all the responses and request that goes to app and from backend express. 

This backend have also admin routes but admin panel is separetly buil din react+vite. 
How both would work. 

As from app when user register through regisetr from a request would generate and it would show to admin panel  but it is not apporved. 

Admin can see the register request for a suer all the data that he fillded via flutter app. you can see all the feilds in docs. 

Admin can click on a request and can see the request and after seeing the request he can and whenhe apporve the request a mail will gnerate automitaclly via smtp server and senf to user that is in the register form. and in this form a poroper message owuld be gnerate via template and have the user email and password. 

and a new user will rregister after approve the request via admin. 

and when user put the email and rendomly genarte password in login page or sign up page he can get login.

And admin can via all the active users and can make user inactive and also can dleete the user. 


