
# School Support Atlas Admin UI

Cookie-authenticated admin panel for School Support Atlas volunteer registration review.

## Running the code

Run `npm install` to install the dependencies.

Run `npm run dev` to start the development server at `http://127.0.0.1:3001`.

Set `VITE_API_BASE_URL` when the backend is not available through `/api`.
